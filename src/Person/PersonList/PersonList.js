import React, { Component } from "react";
import { withRouter } from "react-router-dom";

class PersonList extends Component {
  render() {
    return <div>Person List Page</div>;
  }
}

export default withRouter(PersonList);
